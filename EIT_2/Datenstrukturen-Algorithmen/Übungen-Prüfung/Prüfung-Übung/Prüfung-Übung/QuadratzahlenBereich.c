#include <stdio.h>
#include <math.h>

int count_square_numbers(int A, int B) 
{
	int x = 0;
	for (int i = 1; i < B; i++) 
	{
		
	}
}

int main() 
{
	int A, B;
	scanf_s("%d", &A);
	scanf_s("%d", &B);

	int result = count_square_numbers(A, B);
	printf("%d\n", result);

	return 0;

}